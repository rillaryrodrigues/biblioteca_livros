# Configuração do banco de dados MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/biblioteca_digital?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Gabi@026597

# Configuração do pool de conexões HikariCP
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# Configuração do JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
# opções: none, update, create, create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Gerenciamento de transações
spring.transaction.default-timeout=30

# Ativar/Desativar o log do Hibernate
# spring.jpa.properties.hibernate.format_sql=true

# Outras configurações do Spring Boot
server.port=8080
# Porta do servidor (opcional, se quiser mudar)
